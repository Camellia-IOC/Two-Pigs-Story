spring:
  application:
    name: two-pigs-story-backend-service-user
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        namespace: dev
        prefix: service-user-config
        file-extension: yaml