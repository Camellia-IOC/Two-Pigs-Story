spring:
  application:
    name: two-pigs-story-backend-service-auth
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        namespace: dev
        prefix: service-auth-config
        file-extension: yaml