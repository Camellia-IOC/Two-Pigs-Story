spring:
  application:
    name: two-pigs-story-backend-service-social
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        namespace: dev
        prefix: service-social-config
        file-extension: yaml